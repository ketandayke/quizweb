{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "A quiz application built with Flask and Tailwind CSS",
  "main": "app.js",
  "scripts": {
    "dev": "npx tailwindcss -i ./static/tailwind.css -o ./static/style.css --watch",
    "build": "npx tailwindcss -i ./static/tailwind.css -o ./static/style.css --minify",
    "start": "flask run"
  },
  "dependencies": {},
  "devDependencies": {
    "tailwindcss": "^3.0.0"
  }
}
